# Python
venv/
__pycache__/
*.py[cod]
.ipynb_checkpoints/

# Training images -- 300 SDSS cutouts, re-downloaded by 01_train.ipynb.
# The demo galaxies in data/demo/ ARE committed, so the live notebook runs
# straight from a clone with no internet.
data/train/

# Private planning notes -- not for the public repo
*.pdf

# Weights & Biases run logs (the checkpoint itself is committed)
wandb/

# Figures written by the notebooks on each run (preview.png is committed
# for the README; the rest are your demo-day fallback, made locally)
figures/*.png
!figures/preview.png
